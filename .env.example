OPENAI_API_KEY=key

# Option A) Java(Spring)와 동일하게 사용
# 예: 127.0.0.1:5432/postgres 또는 db-host:5432/postgres
DB_URL=127.0.0.1:5432/postgres
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Option B) Python 개별 설정
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=postgres
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_URL=host.docker.internal:5432/postgres
# 또는
DB_HOST=host.docker.internal
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres

# 추천 모드: fast(5초 목표, 기본) | accuracy(정확도 우선)
RECOMMEND_MODE=fast

# OpenAI 호출 타임아웃(초). 선택.
OPENAI_TIMEOUT_SECONDS=12
# 채팅 모델. 선택(기본 gpt-4o-mini).
CHAT_MODEL=gpt-4o-mini

# 엑셀 ingest 시 사용. 비우면 POST /api/v1/ingest-employment-training 호출 시 503.
INGEST_EXCEL_PATH=/home/kosa/Backend/상담리스트_가공데이터_202602.xlsx

# 시드 스크립트(seed_from_csv.py)용. CSV가 있는 폴더 경로. 필수(비우면 스크립트 에러).
DATABASE_DIR=/path/to/Backend/database